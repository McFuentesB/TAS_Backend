spring.application.name=bff

microservices.usuario-url=${USUARIO_SERVICE_URL:http://localhost:8081}
microservices.profesion-url=${PROFESION_SERVICE_URL:http://localhost:8082}
microservices.cita-url=${CITA_SERVICE_URL:http://localhost:8083}

# Azure AD B2C CIAM issuer - Update with your tenant's issuer URI
# For CIAM, the issuer-uri should match the iss claim in the JWT token from Azure AD
# Temporarily commenting out to allow public endpoints without JWT validation
# spring.security.oauth2.resourceserver.jwt.issuer-uri=https://tuhoraya.ciamlogin.com/

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/tas_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:secret}
spring.jpa.hibernate.ddl-auto=update

# Enable debug logging for security
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG